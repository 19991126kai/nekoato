# nekoato プロジェクト ルール

このリポジトリで作業する Claude（および kai 自身）が従うルール集。

---

## コミットメッセージ

形式は **Conventional Commits**、本文は **日本語** で書く。

### フォーマット

```
<type>: <要約>
```

オプションでスコープ付与:

```
<type>(<scope>): <要約>
```

### type 一覧

| type       | 用途                       | 例                                                |
| ---------- | -------------------------- | ------------------------------------------------- |
| `feat`     | 新機能の追加               | `feat: アイキャッチ画像対応を追加`                |
| `fix`      | バグ修正                   | `fix: 日付フォーマットがズレていたのを修正`       |
| `style`    | 見た目調整（CSSなど）      | `style: ヘッダーの余白を調整`                     |
| `refactor` | 動作変えずコード整理       | `refactor: タグ集計ロジックを関数化`              |
| `docs`     | ドキュメント更新           | `docs: READMEに運用フローを追記`                  |
| `chore`    | メンテ・依存・設定         | `chore: Astroを6.3にアップデート`                 |
| `post`     | 記事の追加・更新（ブログ専用） | `post: 「ある雨の夜の本」を公開`               |

### 書き方の細則

- subject は **50字以内目安**
- 句点（`。`）は付けない
- 動詞形「〜を追加 / 〜を修正 / 〜を更新」
- 詳細が必要なら 1行空けて本文（body）に書く
- 過去のコミットは書き換えない（本ルールは導入以降のコミットに適用）
